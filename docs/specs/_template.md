# {{작업명}}

- 작성일: {{YYYY-MM-DD}}
- 상태: Draft | Approved ← 사람이 승인하면 Approved로 바꾼다. **Approved 전에는 plan·구현으로 넘어가지 않는다.**
- 승인일: {{YYYY-MM-DD}} ← 승인된 날 (Draft면 비워 둔다)
- 승인자: {{이름}} ← 이 spec을 승인한 사람 (Draft면 비워 둔다)
- 최종 위험등급: {{Fast | Standard | High-Risk}} ← 사람이 확정한다. 미지정이면 Standard로 취급.
  초안의 초기 등급과 달라졌으면 그 이유를 적는다. 하향은 물론 유지·상향도 사람이 최종 확인한다.
- 확정 설계: docs/design/{{주제}}.md ← 독립 설계 비교·사용자 결정 후 채운다. Draft 시점엔 비워 둔다.

## 목적
{{무엇을 왜 만드는가 — 한두 문장}}

## 요구사항
- {{해야 하는 것}}

## 완료 조건
이 spec의 완료 조건은 설계와 독립적이다 — Claude·Codex 두 독립 설계안이 같은 기준으로 비교되도록,
요구사항을 정상·오류·경계값까지 확인 가능한 형태로 구체화한다. 검토자(Codex)와 사람이 이 기준으로 판정한다.
- [ ] {{예: 잘못된 입력이면 400과 에러 메시지를 반환한다}}

## 범위 밖
- {{이번 작업에서 하지 않는 것}}

## 변경 이력
요구사항이 바뀌면 코드보다 문서를 먼저 고친다. 목적·범위·핵심 기능이 바뀌면 재설계(독립 설계 비교)부터,
검증 세부 조건만 바뀌면 이 spec부터 고치고 여기에 기록한다.
- {{YYYY-MM-DD}}: {{무엇을 왜 바꿨는지}}
