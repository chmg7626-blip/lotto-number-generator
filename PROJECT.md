# 로또번호생성기 — 프로젝트 상태판

이 파일은 **프로젝트 정체성과 현재 상태의 단일 요약판**이다. 폴더를 옮기거나 새 세션·새 컴퓨터에서
시작해도 이 파일만 읽으면 맥락을 이어받을 수 있게 한다.
행동 규칙은 `CLAUDE.md`, Codex 역할·검토 규칙은 `AGENTS.md`, **현재 상태는 여기**에 둔다.

> 긴 작업 로그·대화 원문·AI 답변 전체를 여기 넣지 않는다. **요약 상태만** 둔다.

## 정체성

- 프로젝트명: 로또번호생성기
- 한 줄 설명: 과거 당첨 데이터 기반 통계와 여러 생성 모드를 갖춘 정적 웹 로또번호 생성기
- 프로젝트 타입: software

## 현재 상태

- 제품 단계: **공개 배포·운영 중**(2026-07-07) — https://chmg7626-blip.github.io/lotto-number-generator/
  이날 하루에 PR #5(실데이터·배포, 저장소 공개 전환)·#6(단골 빈도 설명)·#7(효과음 Web Audio)·
  #8(출력 지연 보상)·#9(BGM 재도입·컷인 교체)·#10(BGM 첫 클릭 시작·홈 음소거 토글) 전부
  main 병합·배포 검증. update-data.yml "변경 없음" 경로 실동작 확인.
  이전: 전문가 훈수 PR #4, 효과음 PR #3(2026-07-05), 추첨 애니메이션 PR #2, 1차 MVP PR #1.
- **알려진 문제(사용자 제보 2026-07-07 마감 시)**: ① 소리 오류가 아직 좀 있음(구체 증상
  미청취 — 다음 세션에 증상부터 확인), ② 모바일 기본 웹 UI는 로컬 구현·화면 검증까지 완료했고,
  실제 키보드 수동 점검·같은 SHA의 CI·독립 리뷰·공개 반영은 아직 남아 있다.
- 콘텐츠 단계: 해당 없음
- 활성 spec: `docs/specs/mobile-draw-stage-ui.md` (Approved, 2026-07-15)
- 현재 브랜치: `codex/mobile-web-ui`

## 진행 중인 챌린지/연재 (없으면 "없음")

- 이름: 없음
- 채널: 없음
- 연재 위치: 없음
- 마지막 로그: 없음

## AI 도구 역할 (이 프로젝트에서)

- Claude Code: 주 진행자 — 요구사항 정리, spec 초안, 독립 설계안 A, 구현, 테스트
- Codex: 독립 설계안 B + 구현 후 독립 검토 (하네스 내부 게이트)
- 외부 조사·자문: 사용자가 외부에서 직접 실행. 특히 당첨번호 데이터 출처·연금복권 규칙·데이터 이용약관 확인에 사용 (출처 있는 사실 근거 / 검증 전 의견 구분)

## 최근 결정

- (2026-07-15) **모바일 기본 웹 UI**: 사용자가 “기본 웹 구조 + 로또 포인트”와 Standard 범위를
  확정. 헤더→소개/최신 번호→생성→결과→정보 흐름으로 재구성하고, 일반 화면의 장식 추첨기는
  제거했다. 320·390·601·641·768·1280px에서 가로 넘침 없이 화면과 생성·결과 흐름을 확인했으며,
  lint·typecheck·113개 테스트·production build는 로컬에서 통과했다. 인앱 브라우저가 키보드
  이벤트 주입을 전달하지 않아 실제 Tab/Enter/Space 수동 점검은 UNVERIFIED로 남긴다. 배포와
  원격 반영은 실행하지 않았다.
- (2026-07-07) **소리 3부작**: ① "매번 늦음" → 재생 계층 HTMLAudio→**Web Audio**(PR #7,
  디코드 전 play 버리지 않기 — Codex 치명), ② "살짝 늦음" → **outputLatency 출력 지연 보상**
  (PR #8, 소리 타이머를 화면보다 먼저), ③ **BGM 재도입**(PR #9, 사용자 제공 음원·게인 0.35·
  연출 중 정지) + 컷인을 CC-BY 팝으로 교체(크레딧 상시 노출, 선행 무음 0.41s 트림) →
  BGM 시작 제스처 pointerdown→**첫 클릭(capture)** 수정 + 홈 음소거 토글 복원(PR #10)
- (2026-07-07) **저장소 공개 전환**: 무료 플랜에서 GitHub Pages는 공개 저장소만 가능 — 사용자
  승인으로 전환. git 이력의 Pixabay BGM 잔존·전 회차 JSON 재배포 위험도 함께 공개됨(기존
  위험 감수 결정의 연장). PR #5·#6 병합, Pages 배포·주간 갱신 실동작까지 검증 완료
- (2026-07-07) 단골 빈도 설명: 목업 3안 비교 후 **B안(게임 아래 설명 한 줄)·frequent 모드
  한정·Fast 사람 확정**(독립 설계·Codex 검토 생략). 게임당 상위 2개 언급, 동률은 공동 순위,
  수치는 draws 집계(자동 갱신 추종), 은/는 조사는 끝자리 규칙. 당첨금 만원 미만 절사는
  "약식 표기" 명시로 해소(Codex 권장)
- (2026-07-06) 실데이터·배포: 출처는 동행복권 공식(조사: docs/research/lotto-draw-data.md).
  **전체 회차 JSON 공개 재배포는 약관 리스크를 사용자가 인지하고 감수** — 완화로 출처·비공식·
  삭제 수용 문구 상시 노출. 위험등급 Standard(배포 인프라지만 정적 호스팅 — 사람 확정).
  설계는 절충안(A 골격: 공용 검증·증분 갱신·단일 CI 파이프라인 + B 통제: diff allowlist·
  prize-round 일치 검증·atomic write). 취득은 스파이크로 발견한 **공식 페이지 JSON API**
  (selectPstLt645Info.do)로 확정 — 엑셀 계획을 사용자 승인으로 변경(구 common.do API는 폐기됨)
- (2026-07-05) 전문가 훈수(패러디): 1차에서 미룬 기능을 **Fast 등급으로 사람 확정**(동작 추가지만
  문구 표시 전용 — 독립 설계·Codex 검토 생략, 요청이 곧 승인). 멘트 16개 랜덤(뽑기 시점 확정),
  '당첨·보장·확률을 높' 금지어는 테스트로 차단. 함께 정리: 생일 조합은 행운수 고정으로 해소,
  미룸 목록에서 제외
- (2026-07-05) 추첨 연출 사운드: 독립 설계 비교 후 **절충안**(A 골격 단일 play API + B의 제스처
  세션 시작·BASE_URL 참조). **BGM은 3차 변경 끝에 제거**(연출 구간→홈 배경음→제거 — "산만").
  팡파르는 결과 컷 진입 → **마지막 공 컷인 동기**로(드럼롤 1.1초 트림, 건너뛰기는 결과 컷에서
  1회 — Codex [치명] 지적을 사용자 판단으로 유지+spec 명확화). 음원은 사용자 다운로드를 ffmpeg
  mp3 변환(효과음 4종 CC0, Pixabay BGM 위험은 제거로 해소 — git 이력엔 잔존). 번외: 용지에
  동행복권 공식 구매 링크(Codex [권장] 분리 지적 → 유지 확정, 별도 커밋)
- (2026-07-03) 추첨 애니메이션 설계·구현: brainstorm 4개 갈림길 결정(첫 게임만 풀 연출·풀스크린
  오버레이·건너뛰기 버튼·결과 컷 후 사용자 닫기) → 독립 설계 비교 후 **절충안**(Claude A의
  연출용 셔플·도메인 무변경 + Codex B의 확인 후 용지 반영·focus 처리). reduced-motion은 JS 분기로
  오버레이 생략. 컴포넌트 테스트는 새 라이브러리 없이 React.act. 미확인: ≤480px MQ 실뷰포트 검증
  (브라우저 한계), reduced-motion 실브라우저 동작
- (2026-07-02) WP-010 완료 + Codex 검토 반영: 모바일 폭에서 당첨금액 2~5등 세로 분해 → `@media(max-width:480px)` 1열. Codex [치명](빈도 그리드가 "높낮이 시각 구분" 미충족 — 목업도 놓침) → 공 아래 빈도 비례 막대 추가(`barPercent`+테스트, design 보정). 재검토 통과. draft PR #1·CI 통과. **CI 미실행 원인=계정 이메일 미인증**, 인증 후 재push로 해결
- (2026-06-30) 생성 모드 재구성: "자주 나온 번호"→**"역대 단골 번호"**('당첨' 단어 제거 — 사행성 규칙), "기념번호 조합"(드물게 가중) **제거** → **"행운수 고정"**(1~45판 0~5개 직접 고정, 나머지 랜덤, ×N 시 고정수 전 게임 공통). 도메인 generateWithFixed 추가·rare 제거(WP-011 done). spec 재Approved·design 직접 갱신
- (2026-06-29) 확정 목업 반영 범위 변경: 저장(localStorage)→복사 대체(savedResults 모듈 삭제), 모드 3종(순수/자주/기념번호), ×1~5 연속뽑기(5게임 용지), 회차 당첨번호 띠·당첨금액(샘플), 빈도 공 그리드, **생성에서 보너스 제거(본번호 6개만)**. 추첨 애니메이션은 별도 spec(범위 밖)
- 형태/스택: React + Vite + **TypeScript** 정적 웹앱, GitHub Pages 공개 배포 지향 (피드백 수집 목적)
- 언어 TS 확정: 독립 설계 비교에서 Codex 안 채택. 학습 목적 + 데이터 스키마 타입 명시. (CI에 typecheck 추가)
- 가중치 공식 확정: 자주=count+1, 드물게=maxCount-count+1 / 빈 데이터 시 랜덤 폴백 / RNG 주입으로 결정적 테스트
- 데이터 출처: 정적 JSON 파일 동반 방식 (실시간 API 대비 안정적, 통계엔 충분). 출처는 외부 조사로 확인 필요
- 위험등급: Standard (풀 워크플로우 — 학습 목적 포함)
- 1차 MVP 범위 좁힘: 로또 6/45 랜덤 생성 + 빈도 통계 + 빈도가중 생성 + localStorage 저장 + 면책 문구
- 2차 이후로 미룸: 연금복권 모드, 다양한 분석(구간·홀짝·연속·추세). (해소됨: 생일 조합→행운수 고정으로 대체 2026-07-05, 전문가 패러디→PR #4로 구현 2026-07-05)
- 사행성 면책 필수: "당첨 보장/확률 향상" 표현 금지, 통계는 예측 아님(도박꾼의 오류 주의)

## 다음 할 일

- **① 소리 오류 잡기(다음 세션 시작점)**: 사용자에게 구체 증상부터 청취(어떤 소리가, 언제,
  어떻게 이상한지) → debug 절차. 후보 맥락: Web Audio 전환·지연 보상·BGM 수명주기 모두 이날 변경
- **② 모바일 기본 웹 UI 검증 마무리**: `docs/specs/mobile-draw-stage-ui.md`와
  `docs/plans/mobile-draw-stage-ui.md` 기준. 실제 키보드(Tab/Enter/Space) 수동 점검, 같은 head
  SHA의 CI, Standard 독립 리뷰와 사람의 공개 반영 승인이 남아 있다.
- 다음 토요일 추첨(1232회) 후 update-data.yml 자동 실행이 새 회차를 실제로 커밋하는지 확인
- BGM 출처 URL 확보 시 docs/research/draw-sound-assets.md 음원 대장 갱신(잔여 라이선스 리스크)
- (선택) 프로젝트 루트 원본 음원 2개(bgm.wav, 810764**mokasza**\*.mp3) 삭제 — 변환본 커밋됨, 사용자 확인
- (선택) 공개 저장소 git 이력에 남은 Pixabay BGM 정리 여부 — 이력 재작성은 사용자만 가능, 위험 감수 시 그대로 둠
- (선택) CI "Node.js 20 deprecated" 경고 해소 — 워크플로우 node-version 22+로 상향
- 진짜 "보너스 공" 추첨 연출을 원하면 생성 로직·spec 변경이 필요한 별개 결정 — 별도 논의
- (선택) 미추적 파일 정리: dgp4d.jpg(용도 미정 마스코트 이미지)·.agents/·.codex/, 폐기된 feature/lotto-mvp-ui-a/-b·병합 완료된 로컬 feature 브랜치 정리 — 사용자 확인 필요

## 주의사항

- 로또는 사행성 소재 — 공개 배포 시 "당첨 확률을 높인다"는 식의 허위·과장 표현을 절대 넣지 않는다.
- "수학적으로 많이 나올법한 번호", 빈도가중 생성은 확률적 근거가 없는 **재미 요소**임을 UI에 명시한다.
- 연금복권은 로또 6/45와 번호 형식이 완전히 달라 별도 데이터·로직이 필요하다 (규칙 확인 필요).
- 당첨번호 데이터 출처의 이용약관·저작권을 확인 전까지 사실 근거로 쓰지 않는다.
