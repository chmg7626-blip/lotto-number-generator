import { describe, it, expect } from 'vitest'

// WP-001 스캐폴딩 검증용 임시 테스트. WP-002에서 도메인 테스트로 대체한다.
describe('scaffolding smoke test', () => {
  it('runs the test runner', () => {
    expect(true).toBe(true)
  })
})
